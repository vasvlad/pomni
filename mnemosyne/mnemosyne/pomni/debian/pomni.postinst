#!/bin/sh -e

PKG="pomni"
FNAME="default.mem"
USER="user"
GROUP="users"

SRC="/usr/share/$PKG/demo/$FNAME"
DESTDIR="/home/$USER/MyDocs/.documents/$PKG"
DEST="$DESTDIR/$FNAME"

if [ "$1" = "configure" -a -r $SRC ]; then
    [ -d $DESTDIR ] || mkdir $DESTDIR
    [ -f $DEST ] || (cp $SRC $DEST && chown -R $USER:$GROUP $DESTDIR)
fi

